/**
 * React Native CLI config.
 *
 * Icon fonts on iOS are bundled via CocoaPods (`@react-native-vector-icons/*` pods).
 * Android fonts are in `android/app/src/main/assets/fonts/`.
 *
 * Do not add icon font folders to `assets` here — it duplicates iOS bundle copies
 * and causes "Multiple commands produce *.ttf" Xcode build errors.
 */
module.exports = {
  assets: [],
};
